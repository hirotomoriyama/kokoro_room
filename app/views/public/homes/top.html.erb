<!--会員ログイン時はログインユーザーの悩み履歴一覧を表示-->
<% if member_signed_in? %>

<div class="container">

  <div class="row my-5">
    <div class="col-6">
      <h2 class="mark text-center rounded"><%= @member.name %>さんの悩み履歴一覧</h2>
    </div>

    <div class="col-6 pl-5">
      <%= link_to new_problem_path do %>
        <h4 class="pl-5"><i class="fas fa-plus-circle"></i> 悩み事を投稿</h4>
      <% end %>
    </div>
  </div>

  <div class="row d-flex flex-wrap">
    <% @problems.each do |problem| %>

    <div class="col-4 bg-light border border-secondary m-5 p-5 rounded">
      <h4>タイトル</h4>
      <!--タイトルをクリック時、悩み事の詳細画面へ遷移-->
      <%= link_to problem.title, problem_path(problem.id) %>

      <h4 class="mt-3">悩みの内容</h4>
      <%= problem.body %>

      <h4 class="mt-3">カテゴリー</h4>
      <%= problem.category.name %>
    </div>

    <% end %>
  </div>

  <div class="row mt-5 pb-5">
    <p class="col-12 text-center pb-5">退会手続きは<%= link_to 'こちら', members_unsubscribe_path %>から出来ます。</p>
  </div>

</div>

<!--会員未登録時、ログアウト時は新規登録、ログインのリンクをそれぞれ表示-->
<% else %>

<div class="container pt-5">

  <div class="row">
    <h2 class="col text-center">利用には会員登録が必要です。</h2>
  </div>

    <div class="row">
      <p class="col text-center">
        ここは会員制のサイトです。<br>
        ご利用の方は<%= link_to 'こちら', new_member_registration_path %>から新規登録して下さい。<br>
        登録済みの方は<%= link_to 'こちら', new_member_session_path %>からログインして下さい。
      </p>
    </div>

</div>


<% end %>