<h2>悩み事の検索結果一覧</h2>

<!--検索結果がある場合-->
<% if @datas.present? %>

  <% @datas.each do |data| %>

  <h3><%= data.member.name %>さんの投稿</h3>

  <h4>タイトル</h4>
  <!--タイトルをクリック後、悩み事詳細画面へ遷移-->
  <%= link_to data.title, problem_path(data.id) %>

  <h4>悩み事の内容</h4>
  <%= data.body %>

  <h4>カテゴリー</h4>
  <%= data.category.name %>

  <% end %>

<!--検索結果が無い場合-->
<% else %>

  検索結果はありませんでした

<% end %>